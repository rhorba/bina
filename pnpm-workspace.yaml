packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts unless explicitly approved here.
# These are native/build deps the web + worker Docker images need at install time.
onlyBuiltDependencies:
  - argon2
  - sharp
  - esbuild
  - "@biomejs/biome"
